name: CyberSecurity Bot CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allow manual triggers

env:
  DOTNET_VERSION: '10.0.x'
  PROJECT_NAME: 'CyberSecurityAwareness'

jobs:
  build-and-test:
    name: Build & Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            framework: net10.0
          - os: windows-latest
            framework: net10.0
          - os: macos-latest
            framework: net10.0

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Restore dependencies
      run: dotnet restore

    - name: Build project
      run: dotnet build --configuration Release --no-restore

    - name: Run tests
      run: dotnet test --configuration Release --no-build --verbosity normal
      continue-on-error: true  # Remove this once you add tests

    - name: Create dummy audio file for testing (if missing)
      if: runner.os == 'windows-latest'
      shell: pwsh
      run: |
        if (-not (Test-Path "greeting.wav")) {
          Write-Host "Creating placeholder audio file..."
          # Create a silent WAV file placeholder
          Add-Type -AssemblyName System.Windows.Forms
        }

    - name: Publish artifact (Windows)
      if: runner.os == 'windows-latest'
      run: |
        dotnet publish --configuration Release --output ./publish/windows --runtime win-x64 --self-contained false
        echo "✅ Build successful for Windows"

    - name: Publish artifact (Linux)
      if: runner.os == 'ubuntu-latest'
      run: |
        dotnet publish --configuration Release --output ./publish/linux --runtime linux-x64 --self-contained false
        echo "✅ Build successful for Linux"

    - name: Publish artifact (macOS)
      if: runner.os == 'macos-latest'
      run: |
        dotnet publish --configuration Release --output ./publish/macos --runtime osx-x64 --self-contained false
        echo "✅ Build successful for macOS"

    - name: Upload build artifacts (Windows)
      if: runner.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: CyberSecurityBot-Windows
        path: ./publish/windows/**
        retention-days: 30

    - name: Upload build artifacts (Linux)
      if: runner.os == 'ubuntu-latest'
      uses: actions/upload-artifact@v4
      with:
        name: CyberSecurityBot-Linux
        path: ./publish/linux/**
        retention-days: 30

    - name: Upload build artifacts (macOS)
      if: runner.os == 'macos-latest'
      uses: actions/upload-artifact@v4
      with:
        name: CyberSecurityBot-macOS
        path: ./publish/macos/**
        retention-days: 30

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Run static code analysis
      run: |
        dotnet tool install -g dotnet-format
        dotnet format --verify-no-changes --verbosity diagnostic

    - name: Check for common issues
      run: |
        # Check for TODO comments
        if grep -r "TODO" --include="*.cs" .; then
          echo "⚠️ Found TODO comments in code"
        fi
        
        # Check file encoding
        find . -name "*.cs" -exec file "{}" \; | grep -v "UTF-8" && echo "❌ Non-UTF8 files found" || true

  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Run security audit
      run: dotnet list package --vulnerable --include-transitive || true

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'table'
        exit-code: '0'
        severity: 'CRITICAL,HIGH'

    - name: Check for hardcoded secrets
      run: |
        if grep -r "password\|secret\|key\|token" --include="*.cs" -i . | grep -v "//"; then
          echo "⚠️ Potential hardcoded secrets detected - please review"
        fi

  create-release:
    name: Create Release
    needs: [build-and-test, code-quality, security-scan]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts

    - name: Create ZIP archives
      run: |
        cd artifacts
        zip -r ../CyberSecurityBot-Windows.zip CyberSecurityBot-Windows/
        zip -r ../CyberSecurityBot-Linux.zip CyberSecurityBot-Linux/
        zip -r ../CyberSecurityBot-macOS.zip CyberSecurityBot-macOS/

    - name: Get version
      id: get_version
      run: |
        echo "VERSION=$(date +'%Y.%m.%d-%H%M%S')" >> $GITHUB_OUTPUT
        echo "TAG_NAME=v$(date +'%Y.%m.%d-%H%M%S')" >> $GITHUB_OUTPUT

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.get_version.outputs.TAG_NAME }}
        name: "CyberSecurity Bot - ${{ steps.get_version.outputs.VERSION }}"
        body: |
          ## 🛡️ Cybersecurity Awareness Bot Release
          
          ### Features
          - Interactive cybersecurity awareness chatbot
          - Password safety tips
          - Phishing awareness education
          - Safe browsing guidance
          - Voice greeting (Windows)
          
          ### Installation
          1. Download the appropriate ZIP for your OS
          2. Extract the files
          3. Run `CyberSecurityAwareness.exe` (Windows) or `dotnet CyberSecurityAwareness.dll` (Linux/macOS)
          
          ### System Requirements
          - .NET 10.0 Runtime
          - Windows: Audio capability for voice greeting
          - Linux/macOS: Console only (audio may not work)
          
          ### Note
          This is an automated build from commit: ${{ github.sha }}
        draft: false
        prerelease: false
        files: |
          CyberSecurityBot-Windows.zip
          CyberSecurityBot-Linux.zip
          CyberSecurityBot-macOS.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
